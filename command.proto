syntax = "proto3";

package Stimulus;

// The command message contains an ID and a numerical value
message Command {
  int32 id = 1;    // Unique identifier for the command
  string type = 2; // Type of command
  int32 number = 3; // The random number or other numerical data
}
